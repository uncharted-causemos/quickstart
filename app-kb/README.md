This is a lightweight setup for Causemos pertaining only to the knowledge explorer.

Use the command `docker-compose up` to start the stack, it may take ~30 seconds before all the services become available.

